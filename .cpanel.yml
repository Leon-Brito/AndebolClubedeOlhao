---
deployment:
  tasks:
    - export DEPLOYPATH=/home/andebol2/public_html/
    - /usr/bin/rsync -a --exclude='.git/' --exclude='.cpanel.yml' --exclude='config.php' --exclude='.env' --exclude='error_log' --exclude='img/equipa/uploads/' ./ $DEPLOYPATH